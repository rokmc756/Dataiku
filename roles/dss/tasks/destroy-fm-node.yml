---
- name: Disable and Stop Systemd Service for Dataiku DSS FM Node
  systemd:
    daemon_reload: yes
    name: dataiku
    enabled: no
    state: stopped
  register: disable_fm_node_systemd_service
  ignore_errors: true
- debug: msg={{ disable_fm_node_systemd_service }}
  when: print_debug == true


- name: Delete Directory for Dataiku FM Design Node
  file:
    path: "{{ _fm.data_path }}"
    state: absent
  register: delete_data_dir
  ignore_errors: true

